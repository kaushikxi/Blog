<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end"> 
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Log Out</a>
        </div>
        <h5 class="">You can edit this post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="block resize-none bg-transparent outline-none bg-zinc-800 bg-transparent p-3 w-1/3 rounded-md mb-2 mt-3" name="content"> <%= post.content %> </textarea>
            <input class="bg-yellow-500 p-3 rounded-md block text-black " type="submit" value="Update post">
        </form>
    </div>
</body>
</html>